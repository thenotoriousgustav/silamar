// Re-export font registration from the resume builder so cover letter PDF
// templates share the exact same font setup (Inter, Roboto, Garamond) without
// duplicating the registration logic.
export * from "@/features/resume-builder/templates/shared/pdf-fonts";
